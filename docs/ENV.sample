# Copy this file to .env and fill values

# --- MongoDB (container init) ---
MONGO_ROOT_USER=
MONGO_ROOT_PASSWORD=
MONGO_INITDB_DATABASE=uitgo_db

# --- JWT / Auth ---
JWT_SECRET_KEY=
ACCESS_TOKEN_EXPIRE_MINUTES=30

# --- TripService client credentials (to get Service Token from UserService) ---
TRIPSVC_CLIENT_ID=
TRIPSVC_CLIENT_SECRET=

# --- UserService base URL (TripService uses to request /auth/token) ---
USER_SERVICE_BASE_URL=http://userservice:8000

# --- Mapbox (TripService) ---
MAPBOX_ACCESS_TOKEN=

# --- VNPay (PaymentService) ---
VNP_TMN_CODE=
VNP_HASH_SECRET=
VNP_URL=https://sandbox.vnpayment.vn/paymentv2/vpcpay.html
# Public base URL to receive VNPay return (e.g., https://<your-ngrok-subdomain>.ngrok.io)
BASE_URL=

# --- Ngrok (optional helper container) ---
NGROK_AUTHTOKEN=
# Note: PaymentService code uses BASE_URL directly; NGROK_API_URL in compose is optional and not read by service.

# --- Service internal URLs (compose defaults) ---
# LocationService
REDIS_URL=redis://redis:6379
# DriverService, TripService, PaymentService, UserService MONGODB_URL are set per service in compose.
